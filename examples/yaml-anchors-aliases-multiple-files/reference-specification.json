{
  "openapi": "3.0.0",
  "info": {
    "title": "Example with anchors and aliases in multiple files",
    "version": "0.0.1"
  },
  "paths": {
    "/aws": {
      "post": {
        "summary": "sample aws-specific route",
        "description": "contains a reference outside this file",
        "security": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["firstName", "lastName"],
                "properties": {
                  "firstName": {
                    "type": "string",
                    "minimum": 1,
                    "maximum": 20
                  },
                  "lastName": { "type": "string", "minimum": 1, "maximum": 20 }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "OK" } }
      },
      "put": {
        "summary": "sample aws-specific route for putting requests",
        "security": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["firstName"],
                "properties": {
                  "firstName": { "type": "string", "minimum": 1, "maximum": 20 }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "OK" } }
      }
    }
  },
  "components": {},
  "tags": []
}
